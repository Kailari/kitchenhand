{
  "name": "kitchenhand",
  "version": "1.0.0",
  "private": true,
  "description": "Simple application for managing weekly menu and shopping list",
  "main": "index.js",
  "author": "Ilari Tommiska",
  "license": "ISC",
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": [
      "**/react-scripts",
      "**/react-scripts/**"
    ]
  },
  "scripts": {
    "test": "yarn workspace server",
    "prestart": "yarn workspace server generate",
    "start": "cross-env NODE_ENV=production tsc && yarn workspace server start",
    "watch": "yarn workspace server watch",
    "pretsc": "yarn workspace server generate && node scripts/buildTs.js",
    "tsc": "tsc",
    "lint": "eslint . --ext ts,tsx"
  }
}
